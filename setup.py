# /workspace/Diploma/setup.py
from setuptools import setup, find_packages

setup(
    name='oneactor',
    version='0.1',
    packages=find_packages(),  # автоматически найдёт OneActor и его подпакеты
)
